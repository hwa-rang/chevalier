// Honor and Reputation are prestige stats — they increase very slowly outside tournaments.
// Max gain per social action: +0.5. Max gain per work action: +0.5.
// Tournaments are the primary source. These values are enforced in activityEffects.ts.
export const MAX_SOCIAL_HONOR_GAIN = 0.2;
export const MAX_SOCIAL_REPUTATION_GAIN = 0.2;
export const MAX_WORK_HONOR_GAIN = 0.3;
export const MAX_WORK_REPUTATION_GAIN = 0.3;
